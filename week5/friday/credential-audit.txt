Requirement 5: Credential Audit — Final Jenkinsfile (week5/friday)
====================================================================
Re-run against the FINAL committed Jenkinsfile, not a reused Thursday
result, since the file changed again today (Lint retry, NEXUS_URL comment).
Paste real output below each command before committing.

--------------------------------------------------------------------
Check 1: No credentials in the Jenkinsfile
--------------------------------------------------------------------
$ grep -i "password\|secret\|token\|pass" Jenkinsfile | grep -v "credentialsId\|Variable\|PASS\|NEXUS"

Expected: no output
Result:
                withCredentials([usernamePassword(

One line matched. This is NOT a credential value — it's the Jenkins
credentials-plugin binding call `usernamePassword(...)` that declares which
credential ID to inject, not a literal secret. It survived the exclusion
filter only because the filter's `PASS` term is case-sensitive and
`usernamePassword` doesn't contain the substring `PASS` in all caps. No
actual password, token, or secret string appears in the file. Treated as a
benign false positive, not a finding.

--------------------------------------------------------------------
Check 2: No .npmrc committed
--------------------------------------------------------------------
$ git ls-files | grep ".npmrc"

Expected: no output
Result:
(empty — no .npmrc file has ever been committed)

--------------------------------------------------------------------
Check 3: Search full git history (this week's commits) for the Nexus password
--------------------------------------------------------------------
$ git log --all -S"<nexus-password>" --oneline

Expected: no output
Result:
(empty in both repos — checked kijanikiosk-devops-foundation AND the
kijanikiosk-payments repo that Jenkins actually builds from. The literal
password never appears in any commit, in either repo's full history.)

--------------------------------------------------------------------
Check 4: Unmasked credential values in any build log this week
--------------------------------------------------------------------
Search the console output for every build this week (Wednesday, Thursday,
Friday runs) for the literal Nexus password string.

Expected: no matches — only **** appears wherever NEXUS_USER / NEXUS_PASS
would otherwise print.
Result:
Checked every build Jenkins currently retains for job `week5-project`:
builds #10–#19 (all from today, Friday). Zero occurrences of the literal
password string in any of the 10 console logs. Spot-checked build #19
directly and confirmed masking is active in practice, not just absent by
coincidence: `+ printf '%s:%s' admin ****` and
`+ NEXUS_TOKEN='YWRtaW46****'`.

Caveat: builds #1–#9 (earlier Wednesday/Thursday runs) no longer exist —
the pipeline's `buildDiscarder(logRotator(numToKeepStr: '10'))` policy
auto-rotated them out once build #20's worth of history accumulated today
during fault-injection testing. Their logs are not retrievable and could
not be audited. No other Jenkins job or log source contains those runs.

--------------------------------------------------------------------
Check 5: No credentials in any other repository surface
--------------------------------------------------------------------
Confirm no credential values appear in PR descriptions, commit messages,
or any file under week5/ (board document, fault log, etc.) committed today.

Result:
`grep -rn "<nexus-password>" week5/` returned no matches. `git log` over
the last 20 commits' messages also returned no matches. No PR exists yet
(see checklist item 7), so there is no PR description to check yet — this
must be re-verified once the PR is opened.

--------------------------------------------------------------------
Summary
--------------------------------------------------------------------
All five checks: PASS, with two noted exceptions (neither is a credential
leak): (1) Check 1's one-line match is the credentials-plugin function
name, not a secret; (2) Check 4 could only cover builds #10-19 because
buildDiscarder pruned earlier builds before this audit ran.
Jenkinsfile version audited: 1e4ec1608d0994706dea4ecbbf727b879f798069
