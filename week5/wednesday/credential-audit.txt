Phase 5: Credential Audit
==========================
Run on the Jenkins/Nexus VM from the repo root, after the four-stage pipeline
has run successfully at least once. Paste the real output below each command
(replace this placeholder text) before committing.

--------------------------------------------------------------------
Check 1: No credentials in the Jenkinsfile
--------------------------------------------------------------------
$ grep -i "password\|secret\|token\|pass" Jenkinsfile | grep -v "credentialsId\|Variable\|PASS\|NEXUS"

Expected: no output
Result:
<paste output here — should be empty>

--------------------------------------------------------------------
Check 2: No .npmrc committed
--------------------------------------------------------------------
$ git ls-files | grep ".npmrc"

Expected: no output
Result:
<paste output here — should be empty>

--------------------------------------------------------------------
Check 3: Search git history for the Nexus password
--------------------------------------------------------------------
$ git log --all -S"<your-nexus-password>" --oneline

Expected: no output
Result:
<paste output here — should be empty>

If this check DOES return a commit: the password was committed to version
control at some point.
  1. Run `git log -p --all -S"<password>"` to find exactly which commit and file.
  2. If the repo is private: rotate the Nexus password immediately, then
     scrub history (git filter-repo / BFG) before the PR is reviewed.
  3. If the repo is public: treat the credential as compromised regardless
     of history rewriting — rotate it immediately.

--------------------------------------------------------------------
Check 4: Unmasked credential values in the Jenkins build log
--------------------------------------------------------------------
Manually search the console log for the build (Jenkins > kijanikiosk-payments
> Build #<N> > Console Output) for the literal Nexus password string.

Expected: no matches — only **** appears wherever NEXUS_USER / NEXUS_PASS
would otherwise be printed.
Result:
<confirm here, e.g. "Searched build #<N> console output for the password
string — zero matches. Only '****' appears at the credential-masking test
stage (see publish-build-log.txt line <N>).">

--------------------------------------------------------------------
Summary
--------------------------------------------------------------------
All four checks: <PASS / FAIL — note any exceptions>
